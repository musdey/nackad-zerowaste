import { describe, it, expect } from '@jest/globals'
// import sendSms from '../../src/lib/smsService'

describe('test smsservice', () => {
  it('should go through', async () => {
    expect('ok').toBe('ok')
  })
})
